const bobbyAge = 17
const legalAge = 18

// don't touch above this line

const message = bobbyAge > legalAge ? 'Bobby is old enough' : 'Bobby is not old enough';

// don't touch below this line

console.log(message)
